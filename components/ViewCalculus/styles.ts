import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 100,
    justifyContent: 'center',
    padding: 16
  },
  textCalculus: {
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: 52,
    color: 'white',
    textAlign: 'right'
  }
});
